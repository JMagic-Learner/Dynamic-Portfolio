import React from 'react';

function Header() {
    const text = 'This is the header section that will be rendered in REACT';
    return (
    <h1> THIS IS THE HEADER. This is the text: {text} </h1>
    );
}

export default Header;